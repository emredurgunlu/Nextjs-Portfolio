/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export", is for static deploying
  output: "export",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
